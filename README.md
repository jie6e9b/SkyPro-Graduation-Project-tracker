# Task Tracker API

> **Status:** ✅ MVP v1.0 - Production Ready

Система управления задачами с учетом времени для команд разработки проектной документации.

Полнофункциональный REST API для управления задачами, подзадачами и участниками команды с JWT аутентификацией и разграничением прав доступа.

## Технологии

- **Backend:** Django 4.2, Django REST Framework 3.14
- **Database:** PostgreSQL 15
- **Authentication:** JWT (djangorestframework-simplejwt)
- **Documentation:** drf-spectacular (Swagger/ReDoc)
- **Containerization:** Docker, Docker Compose

## Основные возможности MVP v1.0

✅ **Управление пользователями**
- Регистрация и аутентификация через JWT
- Управление профилем
- Список пользователей для назначения задач

✅ **Система задач**
- Создание и управление задачами
- Подзадачи с планируемыми часами
- Статусы задач (new, in_progress, review, completed, cancelled)
- Расчет прогресса выполнения

✅ **Роли и права доступа**
- Постановщик задачи (assigner)
- Соисполнитель (co_executor)
- Наблюдатель (observer)
- Разграничение прав на основе ролей

✅ **Персональные представления**
- Мои задачи (где я участник)
- Мои подзадачи (где я исполнитель)
- Задачи, назначенные мной

✅ **API и документация**
- RESTful API с полной документацией
- Swagger UI для тестирования
- ReDoc для просмотра документации

## Быстрый старт

### 1. Клонировать репозиторий

```bash
git clone <repository-url>
cd SkyPro-Graduation-Project-tracker
```

### 2. Создать .env файл

```bash
cp .env.example .env
# Отредактируйте .env при необходимости
```

### 3. Запустить с Docker Compose

```bash
docker-compose up --build
```

Приложение будет доступно по адресу:
- API: http://localhost:8000/api/v1/
- Swagger UI: http://localhost:8000/api/docs/
- ReDoc: http://localhost:8000/api/redoc/
- Admin: http://localhost:8000/admin/

### 4. Создать суперпользователя

```bash
docker-compose exec web python manage.py createsuperuser
```

## Разработка без Docker

### 1. Создать виртуальное окружение

```bash
python -m venv venv
source venv/bin/activate  # Linux/macOS
# или
venv\Scripts\activate  # Windows
```

### 2. Установить зависимости

```bash
pip install -r requirements/development.txt
```

### 3. Настроить базу данных

Создайте базу данных PostgreSQL и обновите `.env`:

```env
POSTGRES_DB=tasktracker
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
```

### 4. Применить миграции

```bash
python manage.py migrate
```

### 5. Запустить сервер разработки

```bash
python manage.py runserver
```

## API Endpoints (MVP v1.0)

### Аутентификация

```
POST   /api/v1/auth/register/       - Регистрация пользователя
POST   /api/v1/auth/login/          - Получение JWT токена
POST   /api/v1/auth/token/refresh/  - Обновление токена
GET    /api/v1/auth/me/             - Профиль текущего пользователя
PATCH  /api/v1/auth/me/             - Обновление профиля
GET    /api/v1/auth/users/          - Список пользователей
```

### Задачи

```
GET    /api/v1/tasks/                  - Список задач (с фильтрацией)
POST   /api/v1/tasks/                  - Создание задачи
GET    /api/v1/tasks/{id}/             - Детали задачи
PUT    /api/v1/tasks/{id}/             - Полное обновление задачи
PATCH  /api/v1/tasks/{id}/             - Частичное обновление задачи
DELETE /api/v1/tasks/{id}/             - Удаление задачи

GET    /api/v1/tasks/my/               - Мои задачи (где я участник)
GET    /api/v1/tasks/my_items/         - Мои подзадачи (где я исполнитель)
GET    /api/v1/tasks/assigned_by_me/   - Задачи, назначенные мной

POST   /api/v1/tasks/{id}/add_role/    - Добавить участника
POST   /api/v1/tasks/{id}/remove_role/ - Удалить участника
POST   /api/v1/tasks/{id}/add_item/    - Добавить подзадачу
```

### Подзадачи

```
GET    /api/v1/task-items/             - Список подзадач (с фильтрацией)
POST   /api/v1/task-items/             - Создание подзадачи
GET    /api/v1/task-items/{id}/        - Детали подзадачи
PUT    /api/v1/task-items/{id}/        - Полное обновление подзадачи
PATCH  /api/v1/task-items/{id}/        - Частичное обновление подзадачи
DELETE /api/v1/task-items/{id}/        - Удаление подзадачи
```

## Структура проекта

```
task_tracker/
├── config/                    # Конфигурация Django
│   ├── settings/
│   │   ├── base.py           # Базовые настройки
│   │   ├── development.py    # Настройки для разработки
│   │   └── production.py     # Настройки для продакшена
│   ├── urls.py               # Главный роутинг
│   ├── wsgi.py
│   └── asgi.py
├── apps/                      # Django приложения
│   ├── users/                # Аутентификация и пользователи
│   ├── tasks/                # Задачи и подзадачи
│   ├── timelogs/             # Учет времени
│   └── comments/             # Комментарии
├── requirements/              # Зависимости
│   ├── base.txt
│   ├── development.txt
│   └── production.txt
├── docker-compose.yml         # Docker Compose конфигурация
├── Dockerfile
├── manage.py
└── README.md
```

## Полезные команды

### Docker Compose

```bash
# Запустить все сервисы
docker-compose up

# Пересобрать и запустить
docker-compose up --build

# Остановить
docker-compose down

# Логи
docker-compose logs -f web

# Выполнить команду Django
docker-compose exec web python manage.py <command>

# Применить миграции
docker-compose exec web python manage.py migrate

# Создать миграции
docker-compose exec web python manage.py makemigrations

# Создать суперпользователя
docker-compose exec web python manage.py createsuperuser

# Запустить shell
docker-compose exec web python manage.py shell
```

### Тестирование

```bash
# Запустить все тесты
docker-compose exec web pytest

# Запустить с verbose
docker-compose exec web pytest -v

# С покрытием
docker-compose exec web pytest --cov=apps

# Конкретное приложение
docker-compose exec web pytest apps/tasks/tests/

# Только определенный тип тестов
docker-compose exec web pytest apps/tasks/tests/test_models.py
docker-compose exec web pytest apps/tasks/tests/test_permissions.py
docker-compose exec web pytest apps/tasks/tests/test_views.py
```

## Статус разработки

### ✅ MVP v1.0 - ЗАВЕРШЕН

#### Этап 1: Основа проекта ✅

- [x] Django проект с настройками (base/development/production)
- [x] Docker + Docker Compose
- [x] PostgreSQL 15
- [x] JWT аутентификация
- [x] Swagger/ReDoc документация
- [x] Модель User с регистрацией

#### Этап 2: Задачи и подзадачи ✅

- [x] Модели Task, TaskItem, TaskRole
- [x] CRUD API для задач и подзадач
- [x] Permissions и разграничение доступа
- [x] Фильтрация задач по статусу, участникам, датам
- [x] Персональные представления (my, my_items, assigned_by_me)
- [x] Управление участниками (add_role, remove_role)
- [x] Расчет прогресса выполнения
- [x] Тесты для моделей, API и permissions

### 🔄 Что дальше: MVP v1.1

#### Этап 3: Учет времени

- [x] Модель TimeLog (создана)
- [ ] TimeLogSerializer и ViewSet
- [ ] API для логирования времени
- [ ] Статистика план/факт
- [ ] Отчеты по затраченному времени

#### Этап 4: Комментарии

- [ ] Модель Comment
- [ ] API для комментариев к задачам
- [ ] Поддержка вложенных ответов

#### Этап 5: Расширенные возможности

- [ ] Уведомления (email, websockets)
- [ ] История изменений задач
- [ ] Дашборды и аналитика
- [ ] Экспорт данных (CSV, Excel)


## Технические детали

### Архитектура

- **Разделение настроек**: base / development / production
- **Кастомные permissions**: IsTaskParticipant, IsAssigner, IsTaskItemExecutorOrAssigner
- **Оптимизированные запросы**: select_related / prefetch_related
- **Транзакции**: атомарные операции для критичных действий
- **Индексы БД**: оптимизированы для частых запросов

### Модели данных

**Task** - Основная задача
- Поля: title, description, status, dates (planned/actual)
- Source links (JSONField) для ссылок на ТЗ
- Расчет прогресса на основе подзадач
- Свойства: progress_percentage, total_planned_hours, total_spent_hours

**TaskRole** - Роли участников
- Связь многие-ко-многим через промежуточную таблицу
- Валидация: только один постановщик на задачу
- Типы ролей: assigner, co_executor, observer

**TaskItem** - Подзадачи
- Executor (назначенный исполнитель)
- Planned hours для планирования
- Статусы: todo, in_progress, completed, blocked
- Автоматическая установка completed_at

**TimeLog** - Учет времени (модель готова, API в разработке)
- Связь с Task и TaskItem
- Логирование часов по дням
- Валидация данных

### Безопасность

- JWT токены (access: 60 мин, refresh: 7 дней)
- Password validators Django
- Разграничение доступа на уровне API
- CORS настроен для безопасного кросс-доменного доступа

### Качество кода

- ✅ Тесты для моделей, permissions, API views
- ✅ pytest + pytest-django
- ✅ flake8 для контроля стиля
- ✅ Автоматическая генерация API документации
- ✅ Type hints (где применимо)

## Лицензия

MIT License

## Контакты

SkyPro - Graduation Project
